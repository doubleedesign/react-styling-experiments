import styled from 'styled-components';
import { Container, Row } from 'react-bootstrap';

export const AlbumGroupWrapper = styled(Container)`
    max-width: 80rem;
`;

export const AlbumList = styled(Row)`
`;
