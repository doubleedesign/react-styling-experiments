import styled from 'styled-components';

export const AlbumGroupWrapper = styled.div`
    max-width: 80rem;
    margin: 0 auto;
    padding: 2rem 0;
    box-sizing: border-box;
`;

export const AlbumList = styled.div`
    display: flex;
    flex-wrap: wrap;
`;
